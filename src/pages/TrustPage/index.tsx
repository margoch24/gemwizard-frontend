import { FC, memo } from "react";
import { Layout } from "../../common/components/layout/Layout";

export const TrustPage: FC = memo(() => {
  return (
    <Layout>
      <div></div>
    </Layout>
  );
});

TrustPage.displayName = "TrustPage";
